import * as React from "react";
import { Card, CardContent, CardHeader } from '@material-ui/core';


export const Customer =  () => (
    <Card>
        <CardHeader title="Order" />
        <CardContent>No Oder yet </CardContent>
    </Card>
);